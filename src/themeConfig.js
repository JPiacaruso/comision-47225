import { createTheme } from "@mui/material";

export const customTheme = createTheme({
  palette: {
    primary: {
      main: "#d87d4a",
      secondary: "#CC646A",
    },

    secondary: {
      main: "#fbaf85",
      secondary: "#AE8131",
    },
  },
});
